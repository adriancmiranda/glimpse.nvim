 glimpse.nvim - Inline image preview for Neovim.

 Renders images directly in the terminal via Kitty Graphics Protocol,
 with fallback to Sixel and external panes (WezTerm, iTerm, tmux).

 Basic usage:
 >lua
   require('glimpse').setup()
 <

 Default keymaps (Oil.nvim):
   `<leader>p` - Preview image side by side
   `;`         - Open image (configurable: current tab or new tab)
   `q`         - Close image buffer

 Supported terminals:
   Kitty, Ghostty (inline), WezTerm (pane), iTerm2 (pane),
   xterm/foot/mlterm (Sixel via tmux)

                                                                  *glimpse.nvim*
GlimpseConfig                                                    *GlimpseConfig*
     @see credits https://github.com/folke/snacks.nvim (snacks.image)
     @see source https://www.reddit.com/r/neovim/comments/1e1txpz/some_fun_with_oilnvim_and_wezterm_for_image/
     glimpse.nvim configuration.

    Fields: ~
        {strategy?}      ("auto"|"inline"|"pane")               Rendering method (default: 'auto')
        {auto_open?}     (boolean)                              Automatically render non-text previewable files after opening them (default: false)
        {pane?}          (GlimpsePaneConfig)                    External pane settings
        {inline?}        (GlimpseInlineConfig)                  Inline rendering options
        {float?}         (GlimpseFloatConfig)                   Floating preview sizes
        {keys?}          (GlimpseKeysConfig)                    Configurable keymaps
        {debounce?}      (GlimpseDebounceConfig)                Debounce timings in ms
        {cell_size?}     (GlimpseCellSizeConfig)                Estimated terminal cell pixel size
        {cache?}         (GlimpseCacheConfig)                   Cache settings
        {safety?}        (GlimpseSafetyConfig)                  Safety settings
        {loading?}       (GlimpseLoadingConfig)                 Loading text
        {auto_refresh?}  (boolean)                              Re-render open previews on save (default: false)
        {image?}         (GlimpseImageConfig)                   Image preview settings
        {video?}         (GlimpseVideoConfig)                   Video preview settings
        {archive?}       (GlimpseArchiveConfig)                 Archive preview settings
        {pipelines?}     (table<string,GlimpsePipelineConfig>)  Conversion pipelines by preview type or extension
        {integrations?}  (GlimpseIntegrationsConfig)            Plugin integrations


GlimpsePreviewOptions                                    *GlimpsePreviewOptions*

    Fields: ~
        {target?}  ("auto")                                 Reserved preview target (default: 'auto')
        {window?}  ("float"|"right"|"left"|"bottom"|"top")  Where to open the preview (default: 'float')


GlimpseInlineConfig                                        *GlimpseInlineConfig*

    Fields: ~
        {rerender_on_tab?}  (boolean)  Re-render when returning to an image tab (default: true)
        {close_with_q?}     (boolean)  Map a key to close the image buffer (default: true)


GlimpseOilConfig                                              *GlimpseOilConfig*

    Fields: ~
        {enable?}      (boolean)                                Keymaps in Oil.nvim (default: enabled)
        {open?}        ("edit"|"tabedit"|fun(filepath:string))
        {follow_cwd?}  (boolean)                                Keep the tab cwd in sync with the opened image (default: true)
                                                                 Open images in the current tab, a new tab, or custom logic (default: 'edit')


GlimpsePaneConfig                                            *GlimpsePaneConfig*

    Fields: ~
        {position?}  ("right"|"bottom")  External pane position (default: 'right')
        {size?}      (number)            External pane size in percent (default: 40)


GlimpseFloatSizeConfig                                  *GlimpseFloatSizeConfig*

    Fields: ~
        {width?}   (number|"auto")  Width in columns, or 'auto' to fill the available editor width
        {height?}  (number|"auto")  Maximum height in rows, or 'auto' to fit content within the available editor height


GlimpseFloatKind                                              *GlimpseFloatKind*

    Type: ~
        "markdown"|"archive"|"cert"|"font"|"key"|"sqlite"|"binary"


                                                            *GlimpseFloatConfig*
GlimpseFloatConfig : GlimpseFloatSizeConfig

    Fields: ~
        {markdown?}  (GlimpseFloatSizeConfig)
        {archive?}   (GlimpseFloatSizeConfig)
        {cert?}      (GlimpseFloatSizeConfig)
        {font?}      (GlimpseFloatSizeConfig)
        {key?}       (GlimpseFloatSizeConfig)
        {sqlite?}    (GlimpseFloatSizeConfig)
        {binary?}    (GlimpseFloatSizeConfig)


GlimpseCacheConfig                                          *GlimpseCacheConfig*

    Fields: ~
        {dir?}           (string)  Cache directory for converted images
        {max_age_days?}  (number)  Days to keep cached files (default: 7)


GlimpseSafetyConfig                                        *GlimpseSafetyConfig*

    Fields: ~
        {max_file_size?}  (number)  Maximum bytes to process (default: 50MB)


GlimpseLoadingConfig                                      *GlimpseLoadingConfig*

    Fields: ~
        {text?}  (string)  Text shown while loading


GlimpseImageConfig                                          *GlimpseImageConfig*

    Fields: ~
        {formats?}  (string[])  Supported image extensions


GlimpseVideoFramesConfig                              *GlimpseVideoFramesConfig*

    Fields: ~
        {strategy?}    ("auto"|"batch"|"poll")  Frame extraction strategy for inline playback (default: 'auto')
        {per_second?}  (number)                 Frames per second to extract (default: 10)
        {limit?}       (integer)                Maximum frames to extract (default: 120)
        {width?}       (integer|"auto")         Frame width in pixels for ffmpeg (default: 640). Use 'auto' to match window width.


GlimpseVideoKeysConfig                                  *GlimpseVideoKeysConfig*

    Fields: ~
        {toggle?}         (string)  Keymap to toggle play/pause in an animation buffer (default: '<CR>')
        {seek_forward?}   (string)  Keymap to seek forward 5 seconds (default: 'l')
        {seek_backward?}  (string)  Keymap to seek backward 5 seconds (default: 'h')


GlimpseVideoConfig                                          *GlimpseVideoConfig*

    Fields: ~
        {formats?}  (string[])                     Supported video extensions
        {open?}     (string|fun(filepath:string))  Command or callback for opening videos externally
        {keys?}     (GlimpseVideoKeysConfig)       Keymaps for inline video playback
        {frames?}   (GlimpseVideoFramesConfig)     Inline video playback settings


GlimpseArchiveConfig                                      *GlimpseArchiveConfig*

    Fields: ~
        {formats?}  (string[])  Supported archive extensions


GlimpseKeysConfig                                            *GlimpseKeysConfig*

    Fields: ~
        {preview?}  (string)  Keymap for preview in Oil (default: '<leader>p')
        {open?}     (string)  Keymap for opening in a tab in Oil (default: ';')
        {close?}    (string)  Keymap for closing the image buffer (default: 'q')


GlimpseDebounceConfig                                    *GlimpseDebounceConfig*

    Fields: ~
        {prefetch?}  (number)  ms before pre-converting on cursor move (default: 200)
        {resize?}    (number)  ms before re-rendering on resize (default: 100)


GlimpseCellSizeConfig                                    *GlimpseCellSizeConfig*

    Fields: ~
        {width?}   (number)  Estimated pixels per column (default: 20)
        {height?}  (number)  Estimated pixels per row (default: 40)


GlimpseTelescopeConfig                                  *GlimpseTelescopeConfig*

    Fields: ~
        {enable?}          (boolean)
        {pickers?}         (string|string[]|table)
        {previewer?}       (table)
        {previewer_opts?}  (table)
        {model?}           (boolean)
        {follow_cwd?}      (boolean)                Keep the tab cwd in sync with the previewed file (default: false)
        {image?}           (boolean)
        {video?}           (boolean)
        {markdown?}        (boolean)
        {archive?}         (boolean)
        {sqlite?}          (boolean)
        {font?}            (boolean)
        {cert?}            (boolean)
        {key?}             (boolean)
        {binary?}          (boolean)


GlimpseIntegrationsConfig                            *GlimpseIntegrationsConfig*

    Fields: ~
        {oil?}        (GlimpseOilConfig)                         Keymaps in Oil.nvim (default: enabled)
        {neotree?}    ({enable?:boolean,auto_preview?:boolean})  NeoTree integration config
        {telescope?}  (GlimpseTelescopeConfig)                   Preview in Telescope (default: enabled)


Glimpse                                                                *Glimpse*


M.setup({opts?})                                                       *M.setup*
     Configure the plugin.

    Parameters: ~
        {opts?}  (GlimpseConfig)  Configuration options (merged with defaults)


M._should_use_inline()                                    *M._should_use_inline*


M.can_preview({filepath})                                        *M.can_preview*
     Return whether the file has a known previewer.

    Parameters: ~
        {filepath}  (string)  Absolute file path

    Returns: ~
        (boolean)


M.get_preview_kind({filepath})                              *M.get_preview_kind*
     Return the preview kind that would be used for the file.

    Parameters: ~
        {filepath}  (string)  Absolute file path

    Returns: ~
        (string|nil)  kind


M.can_auto_open({filepath})                                    *M.can_auto_open*
     Return whether a file should replace its source buffer when auto-opened.
     Native text formats remain editable and are available through preview().

    Parameters: ~
        {filepath}  (string)  Absolute file path

    Returns: ~
        (boolean)


M.show({filepath})                                                      *M.show*
     Show a file (selects the previewer automatically).

    Parameters: ~
        {filepath}  (string)  Absolute file path


M.preview({filepath}, {opts?})                                       *M.preview*
     Quick preview (reuses an existing window or opens a float).

    Parameters: ~
        {filepath}  (string)                 Absolute file path
        {opts?}     (GlimpsePreviewOptions)  Preview options reserved for target selection


M.close()                                                              *M.close*
     Close the active preview.


M.is_image                                                          *M.is_image*

    Type: ~
        (fun(filepath:string):boolean)  Check whether the file is a supported image.


M.is_git_lfs_pointer                                      *M.is_git_lfs_pointer*

    Type: ~
        (fun(filepath:string):boolean|nil)  Check whether the file is a Git LFS pointer.


M.is_video                                                          *M.is_video*

    Type: ~
        (fun(filepath:string):boolean)  Check whether the file is a supported video.


M.is_markdown                                                    *M.is_markdown*

    Type: ~
        (fun(filepath:string):boolean)  Check whether the file is a Markdown document.


M.is_archive                                                      *M.is_archive*

    Type: ~
        (fun(filepath:string):boolean)


M.is_sqlite                                                        *M.is_sqlite*

    Type: ~
        (fun(filepath:string):boolean)


M.is_previewable                                              *M.is_previewable*

    Type: ~
        (fun(filepath:string):boolean)  Check whether the file is previewable.


M.is_cert                                                            *M.is_cert*

    Type: ~
        (fun(filepath:string):boolean)  Check whether the file is an X.509 certificate.


M.is_font                                                            *M.is_font*

    Type: ~
        (fun(filepath:string):boolean)


M.is_key                                                              *M.is_key*

    Type: ~
        (fun(filepath:string):boolean)


M.is_model                                                          *M.is_model*

    Type: ~
        (fun(filepath:string):boolean)  Check whether the file is a supported 3D model.


M.get_config()                                                    *M.get_config*
     Return the current configuration.

    Returns: ~
        (GlimpseConfig)


M._get_user_config()                                        *M._get_user_config*
     Return configuration values explicitly provided by the user.
     @return GlimpseConfig


M.get_terminal()                                                *M.get_terminal*
     Detect the current terminal emulator (including inside tmux).
     The result is cached to avoid repeated vim.fn.system calls.
     @return string|nil terminal 'wezterm' | 'kitty' | 'ghostty' | 'iterm' | nil


M.supports_inline()                                          *M.supports_inline*
     Check whether the terminal supports inline rendering (Kitty Graphics Protocol).
     @return boolean


M.supports_animation()                                    *M.supports_animation*
     Check whether the terminal supports Kitty animation protocol (a=f add-frame).
     Equivalent to supports_inline: every terminal that implements the Kitty
     Graphics Protocol for images also implements multi-frame animation.
     @return boolean


M.in_tmux()                                                          *M.in_tmux*
     Check whether we are inside tmux.
     @return boolean


M.normalize_path()                                            *M.normalize_path*
     Resolve a path to its canonical absolute form.
     @param path string
     @return string


M.same_path()                                                      *M.same_path*
     Return true when two file paths refer to the same file.
     @param left string|nil
     @param right string|nil
     @return boolean


M.is_image()                                                        *M.is_image*
     Check whether the file is a supported image.
     @param filepath string
     @return boolean


M.parse_git_lfs_pointer()                              *M.parse_git_lfs_pointer*
     Check whether the file is a Git LFS pointer instead of the real asset.
     @param filepath string
     @return boolean


M.is_git_lfs_pointer()                                    *M.is_git_lfs_pointer*
     Check whether the file is a Git LFS pointer instead of the real asset.
     @param filepath string
     @return boolean


M.is_video()                                                        *M.is_video*
     Check whether the file is a supported video.
     @param filepath string
     @return boolean


M.is_archive()                                                    *M.is_archive*
     Check whether the file is a supported archive.
     @param filepath string
     @return boolean


M.is_sqlite()                                                      *M.is_sqlite*
     Check whether the file is an SQLite database.
     @param filepath string
     @return boolean


M.is_cert()                                                          *M.is_cert*
     Check whether the file is an X.509 certificate.
     @param filepath string
     @return boolean


M.is_previewable()                                            *M.is_previewable*
     Check whether the file is previewable (image, video, certificate, archive, or SQLite).
     @param filepath string
     @return boolean


M.is_font()                                                          *M.is_font*
     Check whether the file is a font.
     @param filepath string
     @return boolean


M.is_plantuml()                                                  *M.is_plantuml*
     Check whether the file is a PlantUML diagram.
     @param filepath string
     @return boolean


M.is_mermaid()                                                    *M.is_mermaid*
     Check whether the file is a Mermaid diagram.
     @param filepath string
     @return boolean


M.is_model()                                                        *M.is_model*
     Check whether the file is a supported 3D model.
     @param filepath string
     @return boolean


M.is_markdown()                                                  *M.is_markdown*
     Check whether the file is a Markdown document.
     @param filepath string
     @return boolean


M.is_key()                                                            *M.is_key*
     Check whether the file is a GPG or SSH key.
     @param filepath string
     @return boolean


M.preview_buf_name()                                        *M.preview_buf_name*
     Build a human-readable preview buffer name.
     @param filepath string
     @param buf? integer  When provided, appended as [buf] to allow multiple buffers for the same file.
     @return string


vim:tw=78:ts=8:noet:ft=help:norl:
